// STUB TEMPORAL. Reemplazá todo este archivo por el contenido real que
// generaste con:
//   npx supabase gen types typescript --project-id TU_PROJECT_ID > este_archivo
//
// Mientras no lo reemplaces, los queries a Supabase van a compilar pero sin
// autocompletado/chequeo de tipos real (todo se trata como `any` permisivo)
// — funciona, pero no tenés la protección de tipos que buscamos al pedir
// los tipos generados en primer lugar.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
